{
  "name": "cairn-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cairn embedded management UI (Svelte SPA, embedded into the server binary via rust-embed).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.2",
    "vite": "^8.0.16"
  }
}
